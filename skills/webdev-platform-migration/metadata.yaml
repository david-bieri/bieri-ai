# Authored metadata for webdev-platform-migration. The adapter derives `name` from the directory;
# it is intentionally not set here.
description: "Plan and execute migrating a web app off an agent sandbox to self-hosted production: portability audit, env cleanup, Render setup. Use when making an app platform-independent or self-hostable."
version: "1.0.0"
created: "2026-06-11"
updated: "2026-06-11"
depends_on: ["webdev:deploy-render"]
used_by: ["admin:family-hub"]
