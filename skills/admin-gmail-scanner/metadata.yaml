# Authored metadata for admin-gmail-scanner. The adapter derives `name` from the directory;
# it is intentionally not set here.
description: "Scan a dedicated Gmail inbox for structured emails, extract calendar items by subject tag or LLM, dedupe, and POST to an app API. Use for scheduled email-intake pipelines."
version: "1.1.0"
created: "2026-06-11"
updated: "2026-06-11"
depends_on: ["admin:cron-agent", "admin:tag-parser"]
used_by: ["admin:family-hub"]
